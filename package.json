{
  "name": "projeto-growtwitter",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx tsc && node ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^5.1.6",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.2"
  }
}
